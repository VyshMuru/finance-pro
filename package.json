{
  "name": "finance-pro",
  "version": "1.0.0",
  "description": "Production-grade personal finance dashboard",
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "concurrently \"cd server && npm start\" \"cd client && npm run dev\" --names \"SERVER,CLIENT\" --prefix-colors \"cyan,green\"",
    "start:server": "cd server && npm start",
    "start:client": "cd client && npm run dev",
    "seed": "cd server && node db/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
